package com.raspi.retail.frontend.util;

public class LoginUtils {
    //TODO: Add function that checks DB for correct username and password. Returns true if correct, false if incorrect.
//    public boolean LoginChecker(String user, String pass){ //This method checks if the entered data in the login form is equal to an account in the database
//        if(){
//            return(true);
//        } else {
//            return(false);
//        }
//    }
}
